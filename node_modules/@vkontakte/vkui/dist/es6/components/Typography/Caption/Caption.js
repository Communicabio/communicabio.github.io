import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import React from 'react';
import usePlatform from '../../../hooks/usePlatform';
import classNames from '../../../lib/classNames';
import getClassName from '../../../helpers/getClassName';
import { ANDROID } from '../../../lib/platform';

var Caption = function Caption(_ref) {
  var children = _ref.children,
      className = _ref.className,
      weight = _ref.weight,
      level = _ref.level,
      caps = _ref.caps,
      restProps = _objectWithoutProperties(_ref, ["children", "className", "weight", "level", "caps"]);

  var platform = usePlatform();
  var captionWeight = weight;

  if (platform === ANDROID) {
    if (weight === 'semibold') {
      captionWeight = 'medium';
    }
  }

  return /*#__PURE__*/React.createElement("div", _extends({}, restProps, {
    className: classNames(getClassName('Caption', platform), "Caption--w-".concat(captionWeight), "Caption--l-".concat(level), {
      'Caption--caps': caps
    }, className)
  }), children);
};

export default Caption;
//# sourceMappingURL=Caption.js.map